<div class="row questions">
  <div class="span8">
    <h1>Get started</h1>
    <%= form_for @user, :id => "js-user-new-form" do |f| %>

      <div class="form-row name">
        <label>What's your email?</label>
        <%= f.text_field :email %>
      </div>

      <div class="form-row nickname">
        <label>What does your mom know you as?</label>
        <%= f.text_field :nickname %>
      </div>

      <div class="form-row">
        <label>What's your mom's email?</label>
        <%= f.text_field :moms_email %>
      </div>

      <div class="form-row">
        <label>How often should we email?</label>
        <%= f.select :frequency, ['Once a week', 'Once every two weeks', 'Once every four weeks'], :prompt => "Please select one" %>
      </div>

      <div class="form-row">
        <%= f.submit "Submit" %>
      </div>

    <% end %>
  </div>
</div>